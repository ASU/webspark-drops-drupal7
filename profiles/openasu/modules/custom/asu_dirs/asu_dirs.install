<?php

/**
 * Future Webspark DB updates here (instead of asu_cas module)
 */

/**
 * Implements hook_enable()
 */
function asu_dirs_enable() {
  // rebuild the registry, in case any of these modules are already enabled
  registry_rebuild();
}