diff --git a/profiles/openasu/modules/contrib/tb_megamenu/css/base.css b/profiles/openasu/modules/contrib/tb_megamenu/css/base.css
index 7d8ebb1..fbf6882 100644
--- a/profiles/openasu/modules/contrib/tb_megamenu/css/base.css
+++ b/profiles/openasu/modules/contrib/tb_megamenu/css/base.css
@@ -28,12 +28,17 @@
   border-top: 1px solid #eeeeee;
 }
 .tb-megamenu .mega > .mega-dropdown-menu {
-  min-width: 200px;
-  display: none;
+  display: block;
+  -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(Opacity=0)";
+  filter: alpha(opacity=0);
+  opacity: 0;
 }
 .tb-megamenu .mega.open > .mega-dropdown-menu,
 .tb-megamenu .mega.dropdown-submenu:hover > .mega-dropdown-menu {
   display: block;
+  -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(Opacity=1)";
+  filter: alpha(opacity=1);
+  opacity: 1;
 }
 .tb-megamenu .mega-group {
   *zoom: 1;
diff --git a/profiles/openasu/modules/contrib/tb_megamenu/js/tb-megamenu-frontend.js b/profiles/openasu/modules/contrib/tb_megamenu/js/tb-megamenu-frontend.js
index 910914e..a80c94e 100644
--- a/profiles/openasu/modules/contrib/tb_megamenu/js/tb-megamenu-frontend.js
+++ b/profiles/openasu/modules/contrib/tb_megamenu/js/tb-megamenu-frontend.js
@@ -49,6 +49,50 @@ Drupal.TBMegaMenu = Drupal.TBMegaMenu || {};
               mm_duration = $(this).data('duration');
             }
           });
+
+          /**
+           * Allow tabbing by appending the open class.
+           * Works in tandem with CSS changes that utilize opacity rather than display none
+           */
+          // If the selected anchor is not in the TB Megamenu, remove all "open" class occurrences
+          $('a').focus(function(event){
+            if(!$(this).parent().hasClass('tb-megamenu-item')) {
+              $('.tb-megamenu .open').removeClass('open');
+              $('.tb-megamenu-item.dropdown-submenu.open').removeClass('open');
+            }
+          });
+
+          $('.nav > li > a, li.mega > a').focus(function(event) {
+
+            // Remove all occurrences of "open" from other menu trees
+            var siblings = $(this).parents('.tb-megamenu-item').siblings();
+            // var siblings = $(this).closest('.tb-megamenu-item.level-1').siblings();
+            $.each(siblings, function(i, v){
+              var cousins = $(v).find('.open');
+              $.each(cousins, function(index, value){
+                $(value).removeClass('open');
+              });
+              $(v).removeClass('open');
+            });
+            // Open the submenu if the selected item has one
+            if($(this).next(".tb-megamenu-submenu").length > 0){
+                if(!$(this).parent().hasClass("open")){
+                    $(this).parent().addClass("open");
+                }
+            }
+            // If the anchor's top-level parent is not open, open it
+            if(!$(this).closest('.tb-megamenu-item.dropdown').hasClass('open') && $(this).closest('.tb-megamenu-item.dropdown').find('.tb-megamenu-submenu').length > 0){
+                $(this).closest('.tb-megamenu-item.dropdown').addClass('open');
+            }
+            // If anchor's parent submenus are not open, open them
+            var parents = $(this).parents('.tb-megamenu-item.dropdown-submenu');
+            $.each(parents, function(i, v){
+              if(!$(v).hasClass('open')){
+                $(v).addClass('open');
+              }
+            });
+          });
+
           var mm_timeout = mm_duration ? 100 + mm_duration : 500;
           $('.nav > li, li.mega').hover(function(event) {
             var $this = $(this);
