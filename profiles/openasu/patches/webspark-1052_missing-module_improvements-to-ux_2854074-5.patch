diff --git a/README.txt b/README.txt
index edfdf3d..4dfd3dd 100644
--- a/README.txt
+++ b/README.txt
@@ -4,22 +4,37 @@ Missing Modules
 This module lists modules that are activated in your database but missing
 from your file system.
 
-These can greatly impact the performance of your drupal 7 site.
-
-For example: 1 missing module lead to 900 file_scan_directory() calls
-resulting in a wasted 250ms every page load.
-
-
-To use either view the site status report
-
-/admin/reports/status
-
-
-or from drush run
-
-drush list-missing-modules (alias drush lmm)
+These can greatly impact the performance of your Drupal 7 site. For example: 1 missing module load
+to 900 file_scan_directory() calls results in a wasted 250ms with every page load!
 
 INSTALL
-=======
+===============
 
 This module can be installed as any other Drupal module, or as a drush module in e.g. ~/.drush/.
+
+USAGE
+===============
+
+After enabling the module, go to the site status report at /admin/reports/status to see the list
+of missing modules.
+
+If there are any missing modules listed, they'll have the following options:
+
+  * Disable - Simply disables the missing module. This does not remove the
+      missing module's superfluous record from the system table, and it does not remove any
+      lingering tables and/or data generated from the database (especially if the module had an
+      .install file containing hook_install and hook_uninstall function calls).
+  * Remove - Removes the missing module record from the database's system table.
+      This does not remove any lingering database tables and/or data created by the module.
+  * Download from drupal.org - Re-download the module and replace it in the directory reported
+      at /admin/modules/status. If the module is no longer desired, 1) disable it at
+      /admin/modules if it's still enabled, 2) uninstall it at /admin/modules/uninstall if it's on
+      that page's uninstall list (to delete any lingering database tables and/or data generated by the
+      module,) and then re-delete the module code. (If the missing module had an .install file,
+      then this is the best option.)
+
+** Drush commands are also available:
+
+* drush list-missing-modules (alias: drush lmm) - Lists the missing modules incorrectly referenced in the DB.
+* drush remove-missing-modules (alias: drush rmm) - Deletes entries in the `system` DB table and removes them
+  from this list.
\ No newline at end of file
diff --git a/missing_module.form.inc b/missing_module.form.inc
index fb6fa39..3275559 100644
--- a/missing_module.form.inc
+++ b/missing_module.form.inc
@@ -12,11 +12,14 @@ function missing_module_disable_confirm($form, &$form_state, $module_name = FALS
 
   if (array_key_exists($module_name, $form_state['missing_modules']['modules'])) {
     $form = confirm_form(array(),
-                  t('This action disables the @module_name module.', array('@module_name' => $module_name)),
-                  'admin/reports/status',
-                  t('Are you sure you want to disable the @module_name module?', array('@module_name' => $module_name)),
-                   t('Disable module'),
-                   t('Cancel'));
+      t('Disable missing module <strong><em>@module_name</em></strong>', array('@module_name' => $module_name)),
+      'admin/reports/status',
+      t('This does not remove the missing module\'s superfluous record from the system ' .
+        'table, and it does not remove any lingering tables and/or data generated ' .
+        'from the database. <br><br>Are you sure you want to disable the ' .
+        'missing @module_name module?', array('@module_name' => $module_name)),
+      t('Disable module'),
+      t('Cancel'));
   }
   else {
     drupal_set_message(t('%module_name is not a missing module and cannot be disabled.', array('%module_name' => $module_name)), 'error');
@@ -36,13 +39,16 @@ function missing_module_disable_confirm_submit($form, &$form_state) {
 
   if (array_key_exists($module_name, $form_state['missing_modules']['modules'])) {
     db_update('system')
-           ->fields(array('status' => 0))
-           ->condition('type', 'module')
-           ->condition('name', $module_name)
-           ->execute();
+      ->fields(array('status' => 0))
+      ->condition('type', 'module')
+      ->condition('name', $module_name)
+      ->execute();
+    drupal_set_message(t('The %module_name module has been set to disabled.',
+      array('%module_name' => $module_name)), 'status');
   }
   else {
-    drupal_set_message(t('%module_name is not a missing module and cannot be disabled.', array('%module_name' => $module_name)), 'error');
+    drupal_set_message(t('%module_name is not a missing module and cannot be disabled.',
+      array('%module_name' => $module_name)), 'error');
   }
 
   $form_state['redirect'] = 'admin/reports/status';
@@ -56,11 +62,21 @@ function missing_module_remove_confirm($form, &$form_state, $module_name = FALSE
 
   if (array_key_exists($module_name, $form_state['missing_modules']['modules'])) {
     $form = confirm_form(array(),
-                  t('This action removes the @module_name module.', array('@module_name' => $module_name)),
-                  'admin/reports/status',
-                  t('Are you sure you want to remove the @module_name module from the system table?', array('@module_name' => $module_name)),
-                   t('Remove module'),
-                   t('Cancel'));
+      t('Remove the <strong><em>@module_name</em></strong>', array('@module_name' => $module_name)),
+      'admin/reports/status',
+      t('NOTE: This only removes the @module_name module\'s superfluous record ' .
+        'from the system table. If the module had an .install file, it is likely ' .
+        'that there are lingering database tables and/or data created by the ' .
+        'module that need to be deleted by properly uninstalling it at ' .
+        '/admin/modules/uninstall after redownloading the module code. <br><br>' .
+        'Are you sure you want to continue and remove the @module_name2 module ' .
+        'from the system table?', array(
+          '@module_name' => $module_name,
+          '@module_name2' => $module_name
+        )
+      ),
+      t('Remove module'),
+      t('Cancel'));
   }
   else {
     drupal_set_message(t('%module_name is not a missing module and cannot be disabled.', array('%module_name' => $module_name)), 'error');
@@ -80,11 +96,14 @@ function missing_module_remove_confirm_submit($form, &$form_state) {
 
   if (array_key_exists($module_name, $form_state['missing_modules']['modules'])) {
     db_delete('system')
-           ->condition('name', $module_name)
-           ->execute();
+      ->condition('name', $module_name)
+      ->execute();
+    drupal_set_message(t('The record for %module_name in the system table has ' .
+      'been removed.', array('%module_name' => $module_name)), 'status');
   }
   else {
-    drupal_set_message(t('%module_name is not a missing module and cannot be removed.', array('%module_name' => $module_name)), 'error');
+    drupal_set_message(t('%module_name is not a missing module and cannot be removed.',
+      array('%module_name' => $module_name)), 'error');
   }
 
   $form_state['redirect'] = 'admin/reports/status';
diff --git a/missing_module.install b/missing_module.install
index c665714..f0a1f39 100644
--- a/missing_module.install
+++ b/missing_module.install
@@ -27,33 +27,41 @@ function missing_module_requirements($phase) {
     }
     else {
       // In the value field list missing module count and paths to modules.
-      $value = $t('Missing modules detected: (@missing_count)<br /><ul>',
-                                                    array('@missing_count' => count($missing_modules)));
-
-      foreach ($missing_modules as $mm_name => $mm_info) {
-        $module_project = isset($mm_info['info']['project'])? $mm_info['info']['project'] : $mm_name;
-        $value .= $t('<li><span title="@module_path">@module_name</span>  | <a href="@module_url">link to module on drupal.org</a> | <a href="@module_disable">disable</a> | <a href="@module_remove">remove</a></li>',
+      $value = $t('Missing modules detected (@missing_count):<br /><ul>',
+        array('@missing_count' => count($missing_modules)));
+      foreach ($missing_modules as $mm_name => $mm_path) {
+        // @TODO - Hide @module_url for non-drupal.org projects
+        $value .= $t('<li><strong>@module_name</strong> (reported directory: @module_path)
+            <br><em>Options</em>:
+            <a href="@module_disable">Disable</a> |
+            <a href="@module_remove">Remove</a> |
+            <a href="@module_url">Download from drupal.org</a></li>',
           array(
-            '@module_path' => $mm_info['path'],
-            '@module_url' => "http://drupal.org/project/$module_project",
-            '@module_name' => $mm_name,
+            '@module_name' => (isset($mm_path['info']['name'])) ? $mm_path['info']['name'] : $mm_name,
+            '@module_path' => $mm_path['path'],
             '@module_disable' => url('admin/reports/status/disable_module/' . $mm_name),
             '@module_remove' => url('admin/reports/status/remove_module/' . $mm_name),
+            '@module_url' => url('http://drupal.org/project/' . $mm_name),
           ));
       }
       $value .= '</ul>';
 
       // Add explanation, url to module page & drush command to resolve problem.
-      $description = "";
-      $description .= $t('This can have a large performance impact (200ms+!), please disable ');
-      $description .= $t(' or re-download these modules: ');
-      $description .= $t('drush dl @module_names', array('@module_names' => implode(array_keys($missing_modules), " ")));
 
+      $module_list = $t('@module_names', array('@module_names' => implode(array_keys($missing_modules), " ")));
+      $description = "";
+      $description .= $t('Incorrect database entries related to missing modules ');
+      $description .= $t('may have a large performance impact on your site (200ms+ ');
+      $description .= $t('per page load)! Disable, remove or re-download (and ');
+      $description .= $t('if no longer wanted properly uninstall) the ');
+      $description .= $t('above-listed modules to fix the issues. Drush commands are ');
+      $description .= $t('available. See the module README.txt file for more information ');
+      $description .= $t('on using this module.');
       $severity = REQUIREMENT_ERROR;
     }
 
     $requirements['missing_module_requirement'] = array(
-      'title' => $t('Check for missing modules in filesystem'),
+      'title' => $t('Missing modules in codebase'),
       'description' => $description,
       'value' => $value,
       'severity' => $severity,
